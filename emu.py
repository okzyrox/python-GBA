import cpu, mem, cfg

instructions = []

def emu(filepath):
    file = filepath
    while True:
        while len(instructions) >= 0:
            for i in instructions:
                return i
        
        # do later when less tired